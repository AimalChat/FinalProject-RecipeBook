import java.util.HashMap;

/**
 * Write a description of class RecipeBookStates here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class RecipeBookStates
{
    // instance variables - replace the example below with your own
    private HashMap<String, RecipeBookState> validCommands;

    /**
     * Constructor for objects of class RecipeBookStates
     */
    public RecipeBookStates()
    {
        // initialise instance variables
        validCommands = new HashMap<>();
        
        // validCommands.put("back", RecipeBookState.);
        // validCommands.put("select", CommandWord.SELECT);
    }

}
